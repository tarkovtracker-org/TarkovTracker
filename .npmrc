# Keep peer dependency resolution non-strict so optional upstream peer mismatches
# (e.g. @codecov/nuxt-plugin declaring nuxt@3.x while we use Nuxt 4) only warn.
strict-peer-dependencies=false
auto-install-peers=true
