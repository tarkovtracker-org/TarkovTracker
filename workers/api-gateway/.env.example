# === Cloudflare Worker type declarations
# Values in this file are placeholders used only by `wrangler types`.
# Production secrets are managed in the Cloudflare dashboard.
SUPABASE_URL=https://type-only.invalid
SUPABASE_ANON_KEY=type-only-placeholder
SUPABASE_SERVICE_ROLE_KEY=type-only-placeholder
IP_HASH_SECRET=type-only-placeholder
